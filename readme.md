# Supreme package manager index

A package manger for premake5.
Why premake5? Because it's the best build system out there. Seriously. Look how simple it is to define build configurations:

```lua
configuration "Debug"
    defines { "DEBUG" }
    symbols "On"
´´´
